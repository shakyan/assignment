How to Run the project

1. Run the ApiAssignmentApplication.java file which has the main method in it.

2. It will start with the default configurations using H2 In memory data base.

3. Use 'http://localhost:8080/api/v1/files/upload', REST API endpoint to upload a file.
